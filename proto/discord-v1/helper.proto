syntax = "proto3";
package discord_v1;
option go_package = "github.com/thirdscam/chatanium-flexmodule/proto/discord-v1";

import "common.proto";
import "discord-v1/discordgo.proto";

service Helper {
    // rpc ResponseMessage(Message) returns (common.Empty);
    // rpc ResponseInteraction(ResponseInteractionRequest) returns (common.Empty);
    // rpc EditMessage(ChatMessage) returns (common.Empty);
    // rpc EditInteraction(EditInteractionRequest) returns (common.Empty);
}